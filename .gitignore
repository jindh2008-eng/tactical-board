# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code 워크트리 (로컬 전용)
.claude/worktrees/

# 설정 파일 저장(scripts/settings-store.mjs) — 이 PC 의 시나리오 · 백업. 저장소에 올리지 않는다
data/

# Claude Code 개인 권한 설정 — 기기마다 다르다(경로·명령 허용 목록). 공유 설정은 settings.json
.claude/settings.local.json

# settings-ui-audit.mjs 산출물 — 스크린샷은 커밋하지 않는다.
# 기준선은 커밋된 스크립트 + audit.json/.md 이며, 이미지는 그 커밋을
# 체크아웃해 스크립트를 다시 돌리면 언제든 재생성된다.
docs/baseline/**/*.png
