/**
 * 추가출동대 구역 키.
 *
 * `출동대 추가`로 직접 만든 출동대가 처음 담기는 좌측 상단 박스다.
 * 로스터 착대 출동대의 출동대현황(`zoneKey: null`)과는 별개의 자리라,
 * 상황판에 배치했다가 어느 쪽으로 옮기든 옮긴 자리에 그대로 표시된다.
 */
export const UNIT_ADD_ZONE = 'unit-add';
