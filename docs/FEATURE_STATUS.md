# FEATURE_STATUS.md — 기능 구현 상태

> 최종 갱신: 2026-08-26 · **코드를 읽어 다시 쓴 것이다**(이전 판은 2026-05-05 기준)
> 상태는 **네 모드**(D-4)로 나눠 적는다. 라우트는 둘뿐이지만 화면은 넷이다.

## 범례

| | 뜻 |
|---|---|
| ✅ | 구현돼 동작한다 |
| ◐ | 동작하지만 미완이거나 조건이 붙는다 |
| ⏸ | 코드는 있으나 **지금 렌더되지 않는다** (의도된 보류) |
| ❌ | 미구현 — 타입만 있거나 스텁 |

---

## 0. 모드별 요약

| 모드 | 상태 |
|---|---|
| **설정모드** `/settings` | ✅ 운영 중. UI 재설계 S-0~S-6 완료 (2026-08-26) |
| **훈련모드(무플)** `/play` | ✅ 운영 중 — **현재 작업 범위** |
| **훈련모드(지휘)** | ❌ 미구현. [MASTER_PLAN.md](MASTER_PLAN.md) §7.1 |
| **분석(창)** | ❌ 사실상 스텁 (30행, `CommandInfo` 하나만 띄운다 — P-6) |

---

## 1. 건물 표시 (훈련모드 무플)

| 기능 | 상태 | 비고 |
|---|---|---|
| 층 자동 생성 (지상/지하) | ✅ | `buildDisplayFloors()` |
| 층 압축 표시 | ✅ | 화점층 ±3층 개별, 나머지 그룹핑 |
| 화재 상태 6단계 | ✅ | extension-peak / peak / seventy / half / initial / complete |
| 방화문 개폐 (층별 + RF) | ✅ | 상태 변경 시 로그 기록 |
| 계단실 연기 3단계 | ✅ | none / weak(배연 중) / full |
| 초기 문 상태 자동설정 | ✅ | 화점층 이하 open, 위·RF closed |
| 훈련 시작 시 화재상태 적용 | ✅ | 설정의 `initialFireStatus` |
| 건물 미리보기 (설정모드) | ✅ | `BuildingPreview` — 표 기반. `buildDisplayFloors` 공유 |
| 코너 작전구역 | ❌ | `CornerFace` 타입만 존재, 사용처 0 |

---

## 2. 출동대 · 차량

| 기능 | 상태 | 비고 |
|---|---|---|
| 편성(진압·구조·구급 + 차량 6종) | ✅ | `buildRoster` |
| 부대명 인라인 편집 | ✅ | `linkedTo` 항목에 전파 |
| 착대 자동 배정 | ✅ | 같은 종류 최대 + 1 |
| 연동 펌프가 부대 착대를 물려받음 | ✅ | 2026-08-26 수정 (P-13) |
| 착대 드래그 이동 | ✅ | 설정모드. 마우스 전용 |
| 빈 착대 자동 압축 | ◐ | **드래그 경로에만 걸려 있다.** 수량을 줄여 비면 구멍이 남는다 |
| 도착 시간 모드 | ✅ | 1~30분 |
| 토큰 이동 (훈련모드) | ✅ | 좌표 0~1 정규화 |
| 터치 드래그 | ◐ | `useTouchDrag` 구현됨. **실기기 검증 미완** (W-0-1) |

---

## 3. 구조대상자

| 기능 | 상태 | 비고 |
|---|---|---|
| 성별·나이·상태 등록 | ✅ | 드롭다운 |
| 위치 — 면 **또는** 층 (배타) | ✅ | 2026-08 배타 선택으로 변경 (P-12) |
| 배치 미리보기 (설정모드) | ✅ | 사람 아이콘 개수 표시 |
| 검색·구조 진행 (훈련모드) | ✅ | `victim-search` 세션 키 |

---

## 4. 현장요소 (이벤트 토큰)

| 기능 | 상태 | 비고 |
|---|---|---|
| 종류별 정의 (화재·가스·전기) | ✅ | 세부 상태 포함, 범례 제공 |
| 이미지 등록 | ✅ | 한 줄 4개, 파일명 전체 표시 |
| 보드 배치·상태 변경 | ✅ | 보드 대비 0~1 좌표 |

---

## 5. 소방시설 · 방수

| 기능 | 상태 |
|---|---|
| 소화전 등록·배치 | ✅ |
| 송수 연결 (SVG 오버레이) | ✅ |
| 방수 표시 · 수위 | ✅ |
| 고가·굴절 전개 | ✅ |

---

## 6. 지휘절차 · 진행상황

| 기능 | 상태 | 비고 |
|---|---|---|
| 지휘절차 등급별 정의 (초·중·고) | ✅ | 설정모드 |
| 훈련 표시 레벨 선택 | ✅ | **시나리오 값이다** (2026-08-25 공통에서 이동) |
| 우측 지휘절차 훈련 박스 | ✅ | `CommandProcedureTrainingBox` — D-5 대체 구현 |
| 체크리스트 편집 (설정모드 레일) | ✅ | 화면을 옮겨도 스크롤 유지 |
| 편성 없는 도착 항목 표시 | ✅ | `(편성없음)` — 설정·훈련 양쪽 |
| **진행상황 관리 (`ChecklistPanel`)** | ⏸ | **`/play`에 렌더되지 않는다.** D-5로 제거, 지휘 화면용으로 보존 |
| `ChecklistDrawer` | ⏸ | 호출부 없음. 지휘 화면용으로 **의도적 보존** |

---

## 7. 로그

| 기능 | 상태 |
|---|---|
| 이벤트 로그 기록 | ✅ |
| 로그 패널 표시 | ✅ |
| CSV 내보내기 | ✅ |
| 체크리스트 Markdown 내보내기 | ✅ |

---

## 8. 훈련 흐름

| 기능 | 상태 | 비고 |
|---|---|---|
| 훈련 세팅 (설정 → 세션) | ✅ | 두 저장소가 만나는 유일한 지점 |
| 시작 · 종료 · 타이머 | ✅ | |
| `runKey` 재마운트 초기화 | ✅ | 상태 초기화 로직을 따로 두지 않는 방식 |
| 준비도 검사 | ◐ | 전부 `warn`. 실행을 막을지는 미결 (Q-2) |

---

## 9. 화면 · 배율

| 기능 | 상태 | 비고 |
|---|---|---|
| 훈련창 스테이지 배율 | ✅ | `StageRoot` 단일 지점. 레터박스 구조적으로 0 |
| 가로/세로 전환 | ✅ | 히스테리시스 1.15 / 0.87 |
| 설정모드 리플로우 | ✅ | 컨테이너 쿼리 3단. 스테이지 미사용 |
| `--ui-scale` 잔재 | ◐ | 훈련모드 CSS에 참조 94건. **정의가 없어 전부 폴백 1로 죽어 있다** (정리 대상) |
| 에러 바운더리 | ✅ | `RouteErrorBoundary`가 Routes를 감싼다. 상단 nav는 밖에 남겨 이동 가능 |

---

## 10. 미구현 · 스텁

| 항목 | 상태 | 근거 |
|---|---|---|
| 훈련모드(지휘) 화면 | ❌ | §7.1 — 다음 큰 단계 |
| 훈련 분석 통계 | ❌ | `AnalysisModal` 30행, `CommandInfo`만 띄운다 (P-6) |
| 지휘방침 표시 | ❌ | `CommandStrategy`·`CommandMethod` 타입만, 사용처 0 |
| 코너 작전구역 | ❌ | `CornerFace` 타입만, 사용처 0 |
| **앱 테스트** | ❌ | 없다. 검증은 브라우저에서 직접 한다 |

> `npm run test:chatgpt-summary`는 `scripts/summarize-chatgpt-project.mjs` 전용이며 앱과 무관하다.

---

## 11. 품질 기준선

| 항목 | 현재 |
|---|---|
| `npx tsc -b` | 통과 |
| `npm run lint` | 오류 약 56건 — **기준선**. 대부분 `react-refresh/only-export-components`(이 코드베이스의 관례)와 `react-hooks` 계열. 파일 단위로 비교해 새 오류만 회귀로 본다 |
| `npm run lint:css` | 통과. 억제 잔여 **14건** — 실제 결함은 `ZoneCell.css` 무효 CSS 2건뿐(P-9), 나머지는 표기 취향 |
| 설정모드 UI 감사 | `scripts/settings-ui-audit.mjs`. 설정 7화면은 §9 목표 달성, 남은 결함 19건은 전부 `ScenarioModal`(§10 범위 밖) |
